See @AGENTS.md for the full agent guide (usage, flags, behavior guarantees, conventions).

To transcode a video, use the `video-optimize` skill (`.claude/skills/video-optimize/SKILL.md`) or run `./optimize.sh` directly.
